// import { Controller, Logger } from '@nestjs/common';
// import { EventPattern, Payload } from '@nestjs/microservices';

// @Controller('eventi')
// export class EventiController {
//   private readonly logger = new Logger(EventiController.name);

//   @EventPattern('evento vendita')
//   handleEventoVenditaProdotto(@Payload() data: any) {
//     // Implementa la logica per gestire l'evento di vendita di un prodotto
//     this.logger.log('Evento:', data);
//   }
// }
